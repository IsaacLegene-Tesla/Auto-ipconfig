# Policy history

Each **Fleet build** saves the uploaded policy `.conf` here:

`fleet_push_YYYYMMDD_HHMMSS_<original-name>.conf`

Use this folder to see what policy file was used for past fleet rollouts. The matching per-AMR outputs are in your **Downloads** folder under `Fleet_config_<same-timestamp>/`.
